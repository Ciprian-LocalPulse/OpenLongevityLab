# Release process

1. Work on `principal` and keep changes reviewable.
2. Run Python, Rust, frontend, Docker, and security checks.
3. Open and merge a pull request into `main`.
4. Create an annotated `vX.Y.Z` tag on the merge commit and publish release notes.
