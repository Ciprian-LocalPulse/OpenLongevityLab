# Contributing

Create a focused branch, add tests for behavior changes, document provenance for scientific records, and run `pytest` and `ruff check .` before opening a pull request. Do not commit secrets, private datasets, or unsupported medical claims. Changes to evidence rules require a rationale and fixture-based tests.
