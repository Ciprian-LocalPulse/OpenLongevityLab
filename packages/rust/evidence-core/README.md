# Rust evidence core

This optional crate contains small safe kernels that can later be exposed to Python through a deliberate FFI boundary. The Python implementation remains the reference behavior until cross-language parity tests are added.
